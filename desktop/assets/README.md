# Iconos Facturamas

Esta carpeta contiene los iconos usados por el empaquetado de escritorio:

- `app.icns`: icono para macOS.
- `app.ico`: icono para Windows.
- `app.png`: icono de ventana para Electron.

Los scripts `package:mac` y `package:win` usan estos ficheros mediante `electron-packager`.
